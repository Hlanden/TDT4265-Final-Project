MODEL:
    IN_CHANNELS: 1 #husk å endre i contractblock også
    OUT_CHANNELS: 4 #husk å endre i expandblock også
DATASETS:
    TRAIN_IMAGES: '../../../../work/datasets/medical_project/CAMUS'
MODEL:
    CLASSES: [1, 2, 3]
PREPROCESSING:
    ISOTROPIC_PIXEL_SIZE:
        ENABLE: True
        SIZE: 384
        PROBABILITY: 1

    ELASTICDEFORM:
      ENABLE: True
      ALPHA: 300
      SIGMA: 30
      ALPHA_AFFINE: 1
      PROBABILITY: 0.5
OUTPUT_DIR: 'outputs/UNET_base_model'
LOGGER:
    SAVE_DIR: True